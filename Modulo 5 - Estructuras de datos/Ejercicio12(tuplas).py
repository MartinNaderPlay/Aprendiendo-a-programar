# Convertir la siguiente lista en tupla y luego de nuevo en lista, usando tuple() y list()

lista = [1, 2, 3, 4, 5]

tupla = tuple(lista)

print("La lista convertida en tupla es:", tupla)

lista = list(tupla)

print("La tupla convertida de nuevo a lista es:", lista)
